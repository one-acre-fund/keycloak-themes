initContainers:
 - name: init-myservice
   image: oaftech.azurecr.io/oneacrefund/keycloak-themes:latest
   volumeMounts:
   - name: themes
     mountPath: /usr/keycloak/themes/alfresco

extraVolumeMounts:
- name: themes
  mountPath: /opt/bitnami/keycloak/themes/alfrsco
  subPath: alfresco

extraVolumes:
- name: themes
  persistentVolumeClaim:
    claimName: keycloak-themes
